import Foundation

/// Legacy duplicate formatter kept intentionally empty to avoid colliding with the
/// canonical `Formatters` utility in `Core/Common/Formatters.swift`.
///
/// The active formatting helpers live in:
/// `Crave_IOS/Core/Common/Formatters.swift`
