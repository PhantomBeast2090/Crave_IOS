import Foundation

/// Legacy duplicate file retained as a no-op stub to avoid duplicate type declarations.
/// The active repository contract and implementation live in `RepositoryProtocols.swift`
/// and `AppRepositoryImpl.swift`.
